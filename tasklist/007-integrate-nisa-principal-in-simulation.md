# タスク: NISA元本のシミュレーションへの統合

## 概要

`api-adapter.ts`で計算されたNISAの投資元本を、シミュレーションロジックに統合する。

## 手順

1.  `api/simulate/index.ts`を修正する。
    -   シミュレーションの初期データとして、`initialPrincipal`を受け取るようにする。
    -   初年度のNISA資産額の計算に、元本を基準値として使用する。
    -   資産推移の計算を開始する際に、初年度の資産が「元本」と「評価損益」に分解された状態で初期設定されるようにロジックを調整する。
